Requirements

Make sure you have installed:

Java 17+

Maven 3+

MariaDB or MySQL Server

Bash shell (for running .sh scripts)

Ensure mariadb is up and running

How to start program:

Go to erp folder and run following sh files
./refresh.sh
./setup.sh
./run.sh

Every subsequent time you wish to run program, do run.sh